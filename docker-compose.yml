version: "3.8"

services:
  funding-service:
    container_name: funding-service
    image: ${DOCKER_USERNAME}/funding-service:latest
    ports:
      - "8084:8084"
    env_file:
      - .env
    networks:
      - backend

networks:
  backend:
    external: true